server
{
    listen 8080;
    host 127.0.0.1;
	client_max_body_size 10000;
	root pages/pages2;
	
	location / {
		index homepage.html;
		# allow_methods GET POST ATT;
		autoindex on;
		# root pages;
		upload_dir upload;
	}

	location /cgi-bin{
		root cgi_script;					
        cgi_extension php py pl;
	}

	location /www {
		# root pages;

	}

	location = /lol {
		# root pages;

    }

	location /listFiles {
		# root pages;

	}
}